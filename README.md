# golab
Lightweight CLI tool for defining network topologies via YAML and automatically generating device configurations and Docker-based lab environments. Focused on simplicity, reproducibility, and rapid testing of network setups.

## Why I built this
Setting up network labs manually is slow and error-prone. This tool allows defining topologies declaratively and spinning them up quickly for testing and experimentation.
